\ProvidesPackage{StyleFiles/mystyle}

%\usepackage[a4paper,twoside,left=2.5cm,right=2cm,top=30mm,bottom=25mm,bindingoffset=1.5cm,head=14.5pt]{geometry}
\usepackage[a4paper,twoside,left=2.5cm,right=2cm,top=30mm,bottom=25mm,head=14.5pt]{geometry}

% set font
\usepackage[T1]{fontenc}
\usepackage{lmodern}

%- set "one and a half" line spacing document-wide, but not where it is usually
%unnecessary (e.g. footnotes, captions)
\usepackage{setspace}
%\onehalfspacing
\doublespacing
% line in between paragraphs, and no indent
\usepackage[parfill]{parskip}

%- disable ligatures altogether in the whole document
\usepackage{microtype}
\DisableLigatures{encoding = *, family = *}

\usepackage{graphicx}
\graphicspath{ {Images/}}

\usepackage{ifpdf}
\usepackage{datetime}

\usepackage{xspace}

\usepackage{placeins}

\usepackage{subcaption}

%\usepackage[Lenny]{fncychap}

\usepackage{color}
\definecolor{gray75}{gray}{0.75}

\usepackage[compact]{titlesec}
\newcommand{\hsp}{\hspace{20pt}}
\titleformat{\chapter}[hang]{\Huge\bfseries}{\thechapter\hsp\textcolor{gray75}{$|$}\hsp}{0pt}{\Huge\bfseries}
\titlespacing*{\chapter}{0pt}{-30pt}{40pt}
\titlespacing{name=\chapter,numberless}{0pt}{-30pt}{30pt}
% may need this at some point
% turn of those nasty overfull and underfull hboxes
%\hbadness=10000
%\hfuzz=50pt

% chapters names in LoF and LoT
\usepackage{StyleFiles/chaptersinindex}
\AddChaptersToIndex{lof}{figure}
\AddChaptersToIndex{lot}{table}

% make table of contents, list of figures and list of tables items clickable links 
\usepackage{hyperref}
\hypersetup{linktoc=all} %set to all if you want both sections and subsections linked

% maths stuff
\usepackage{amsmath}
\usepackage[version-1-compatibility]{siunitx}
% setup the SIunits package (more info in 'texdoc siunitx')
\sisetup{number-unit-product = \text{ }}
\sisetup{binary-units = true}
\sisetup{seperr, openerr=, closeerr=}
% avoid conflicts between microtype and siunitx when loading things like \textmu and \textminus
\usepackage{textcomp}


% allow landscape for e.g. wide tables
\usepackage{lscape}
\usepackage{pdflscape} % so landscape pages are rotated in the pdf

% Print URLs not in Typewriter Font                                     
\def\UrlFont{\rm}



% bold small caps fix
%\normalfont
%\DeclareFontShape{T1}{lmr}{bx}{sc} { <-> ssub * cmr/bx/sc }{}

% hyphenation
\hyphenation{brems-strahl-ung}

% Tables handling: adjustable row styles
\newcolumntype{_}{>{\global\let\currentrowstyle\relax}}
\newcolumntype{^}{>{\currentrowstyle}}
\newcommand{\rowstyle}[1]{\gdef\currentrowstyle{#1}
						  #1\ignorespaces
						  }
\renewcommand{\arraystretch}{1.2}

% merge rows in tables
\usepackage{multirow}

%
%
% DECLARATIONS
%
% These macros are used to declare arguments needed for the
% construction of the title page and other preamble.

% The year and term the degree will be officially conferred
%\def\degreedate#1{\gdef\@degreedate{#1}}
% The word count of thesis
%\def\wordcount#1{\gdef\@wordcount{#1}}
% The name of your college or department(eg. Trinity, Pembroke, Maths, Physics)
%\def\collegeordept#1{\gdef\@collegeordept{#1}}
% The name of your University
%\def\university#1{\gdef\@university{#1}}
% Defining the crest
%\def\crest#1{\gdef\@crest{#1}}